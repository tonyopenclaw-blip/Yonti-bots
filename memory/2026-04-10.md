# Daily Notes — 2026-04-10

## Strategy — Back to Original Momentum

Final setup today:
- **>90%** above prev close → BUY YES (CONF=95)
- **<30%** above prev close → BUY NO (CONF=99)
- No Tier 2, no middle zone
- All 8 coins: BTC, ETH, SOL, BNB, DOGE, XRP, HYPE, ADA

Removed Tier 2 entirely after Tony confirmed. Original was cleaner.

## Bugs Fixed Today

1. **Signal file collision (FIXED)** — went from single `candle_signals.json` to per-coin `candle_signals/{COIN}.json`. Each CoinTrader now only reads its own coin's signals.

2. **close_position bug (FIXED earlier)** — `close_side = "no" if side == "yes" else "yes"` → `close_side = position.side`

3. **Discord webhook (BROKEN)** — `uncle-vito` channel webhook returning 401 Invalid Token. Need fresh URL from Tony. Code has it at `candle_watcher.py` line 38 `DISCORD_WEBHOOK`.

## Bot Status

- Restarted at 11:56 UTC after signal routing fix
- Cash: ~$74.32
- Positions: 0 (all closed)
- Realized P&L: ~$39.65 (114.4%)

## Discord Heartbeat Added

- candle_watcher posts to Discord every :00, :15, :30, :45 UTC
- Shows signal status or "Watching - no signals this candle"
- Blocked on bad webhook URL

## Kalshi Auth Issues

- Getting 401s on `/portfolio/balance` endpoint intermittently
- Bot handles it with retry + auth refresh
- Not blocking trading but worth monitoring

## What's Working

- Candle-duration signal (historical edge: 90-99% win rate on ETH/SOL when >90% above prev close)
- Momentum strategy with tight thresholds
- Scale-in on winning trades (RIP)

## Open Issues

1. Discord webhook URL invalid — waiting on Tony for new one
2. Kalshi 401s — may need to re-authenticate at some point


## Evening Update — 2026-04-10

### Kelly Sizing Fix
- **Root cause found:** `_execute_candle_signal` hardcoded `size=1`, bypassing Kelly sizing entirely
- **Fix applied:** Changed to call `calculate_kelly_size(..., cash_override=self.cash)` 
- **Fix location:** `superbot.py` line ~911 in `_execute_candle_signal`
- **Kelly params:** 50% Kelly entry, confidence tiers (CONF 90+ → 1.5x, 80-89 → 1.0x, 60-79 → 0.75x, 40-59 → 0.50x), max 3 contracts
- **Restarted:** 19:32 UTC after fix

### Post-Fix Trading (19:32 UTC)
- BTC NO 3@$0.255, ETH NO 3@$0.235, BNB NO 3@$0.355, SOL NO 3@$0.40, DOGE NO 3@$0.545, HYPE NO 3@$0.645
- Cash spent: ~$10.69 (6 trades × ~$1.78 each)
- **Problem:** All 6 NO trades immediately went wrong — crypto surged YES

### Current Open Positions (all NO, all underwater at 19:35)
| Coin | Side | Entry | Est. Current |
|------|------|-------|--------------|
| BTC | NO | $0.255 | ~$0.26 |
| ETH | NO | $0.235 | ~$0.23 |
| BNB | NO | $0.355 | ~$0.35 |
| SOL | NO | $0.400 | ~$0.40 |
| DOGE | NO | $0.545 | ~$0.54 |
| HYPE | NO | $0.645 | ~$0.65 |

### Regime Problem
- Crypto is in a YES momentum regime
- Candle signals keep triggering NO based on >30% above prev close
- Every NO trade from 19:32 is losing
- DOGE also 0-3 on YES today — wrong direction overall

### Cash State
- Reported: ~$46.40 (synced from paper ~$35.71)
- Discord 401 errors causing CashSync to fall back to paper balance
- Real balance uncertain due to auth issues

### What Needs Attention
1. Open NO positions may need to be cut if regime doesn't shift
2. Discord webhook still broken (401 invalid token) — Tony hasn't provided new URL
3. Cash desync due to 401s on portfolio endpoint
